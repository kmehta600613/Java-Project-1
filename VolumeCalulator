/*
 
The program should be able to ask the user for the dimensions of the cube, and use the correct formula to print the volume of the cube

*/

import java.util.Scanner;
public class ComputeCubeVolume {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
      Scanner keyboard = new Scanner(System.in);
      System.out.print("Enter the length of the cube in cm [decimal number]:");
      double lengthOfTheCube = keyboard.nextDouble();
      System.out.print("Enter the width of the cube in cm [decimal number]:");
      double widthOfTheCube = keyboard.nextDouble();
      System.out.print("Enter the height of the cube in cm [decimal number]:");
      double heightOfTheCube = keyboard.nextDouble();
      double volumeOfTheCube = lengthOfTheCube * widthOfTheCube * heightOfTheCube;
/*
The Scanner class is used to ask for user input for the necessary dimensions of the cube. Those inputs are then stored into variables
for the calculation of the volume of the cube (as shown for the variable volumeOfTheCube).
 */
      System.out.println("");
      System.out.printf("The volume of the cube is %.2f cm^3 with length: %.2f cm, width: %.2f cm, and height: %.2f cm.", volumeOfTheCube, lengthOfTheCube, widthOfTheCube, heightOfTheCube);
/*
The printf statement is used here with the number of decimals specified so that no matter how many decimals the user inputs, the 
numbers will be formatted with the same number of decimals.
 */
    	System.out.println(-1+2-3);	  
	}

}
